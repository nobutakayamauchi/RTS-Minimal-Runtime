# Intent

Prove that RTS can run as a minimal reconstructable flow.

This bootstrap run starts from a human intent, converts it into a minimal spec, records execution, preserves verification evidence, and enables reconstruction of what happened afterward.
